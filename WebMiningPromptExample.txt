Generate 40 multiple-choice questions (MCQs) Web Mining and Design from these slides.

Requirements:
- Match the style and vibe of the sample questions of the JSON file
- Wrong options must be plausible but definitively incorrect (avoid obviously wrong distractors)
- Provide detailed explanations without citation
- Make sure options is 0-indexed

Output format: JSON array with identical structure to the JSON file.